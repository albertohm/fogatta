%h1 Chat

%ul#chat
  - @messages.each do |message|
    %li
      %strong.autor= "[#{message.created_at if message.created_at}] #{message.author}:"
      %span= message.content

= form_for Message.new, remote: true do |f|
  = f.text_field :content
  = f.submit "Send"

= subscribe_to "/messages/new"
